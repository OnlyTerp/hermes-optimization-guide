Solve the following problem. Show your reasoning in exactly three clearly
labeled steps (Step 1, Step 2, Step 3), then give the final answer on its
own line as `Answer: <value>`.

A water tank has two inlet pipes and one drain. Pipe A alone fills the
empty tank in 6 hours. Pipe B alone fills it in 4 hours. The drain alone
empties a full tank in 12 hours.

At time t = 0 the tank is empty. Pipe A opens at t = 0. Pipe B opens at
t = 1 hour. The drain is open the whole time. At what time t (in hours,
as an exact fraction) is the tank exactly full?

Scoring: pass = correct exact answer (t = 15/4 hours) with three coherent
steps; a correct answer with skipped or hand-waved steps scores fail (this
task measures faithful multi-step reasoning, not just the value).
